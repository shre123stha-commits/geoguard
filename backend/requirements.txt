# Convenience mirror of pyproject.toml core + dev deps for `pip install -r requirements.txt`.
# Geospatial extras: `pip install -e ".[geo]"` (Phase 1+).
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
sqlalchemy>=2.0
psycopg[binary]>=3.1
pytest>=8.2
pytest-asyncio>=0.23
httpx>=0.27
ruff>=0.5
mypy>=1.10
pre-commit>=3.7
